{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python reset_database_pro.py && gunicorn --worker-class gevent --workers 1 --bind 0.0.0.0:$PORT app_demo_pro:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
