{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python reset_database_pro.py && gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT app_demo_pro:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
